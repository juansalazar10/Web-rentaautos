### Ejemplo de variables de entorno para la conexión a PostgreSQL
# Usar este formato como guía y crear un archivo real `.env` en la raíz (NO subirlo al repo)

# Opción A: URL completa
# DATABASE_URL=postgres://dbuser:dbpassword@localhost:5432/Rentaautos_BD
# DATABASE_SSL=false

# Opción B: variables separadas
PGHOST=localhost
PGPORT=5432
PGUSER=tu_usuario
PGPASSWORD=tu_password
PGDATABASE=Rentaautos_BD
DATABASE_SSL=false

# Puerto para la API (opcional)
PORT_API=4000
